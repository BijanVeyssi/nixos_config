{
  # List your reusable home-manager modules here, exported through the flake's
  # `homeManagerModules` output. These are usually things you would upstream
  # into home-manager eventually.
  #
  # example = import ./example.nix;
}
