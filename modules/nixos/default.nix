{
  # List your reusable NixOS modules here, exported through the flake's
  # `nixosModules` output. These are usually things you would upstream into
  # nixpkgs eventually.
  #
  # example = import ./example.nix;
}
